diff --git a/dist/_browser-chunks/chunk-DWOHA7QX.js b/dist/_browser-chunks/chunk-DWOHA7QX.js
deleted file mode 100644
index da164adc1b60f5b4a4f2552cb8156d0492071543..0000000000000000000000000000000000000000
diff --git a/dist/_browser-chunks/chunk-RT3VPW6U.js b/dist/_browser-chunks/chunk-RT3VPW6U.js
new file mode 100644
index 0000000000000000000000000000000000000000..8aab3d5447e56771eb86aad81883a99585e59f73
--- /dev/null
+++ b/dist/_browser-chunks/chunk-RT3VPW6U.js
@@ -0,0 +1,204 @@
+import {
+  __export
+} from "./chunk-4BE7D4DS.js";
+
+// src/preview.tsx
+var preview_exports = {};
+__export(preview_exports, {
+  decorators: () => decorators,
+  loaders: () => loaders,
+  parameters: () => parameters
+});
+
+// src/routing/decorator.tsx
+import React2 from "react";
+import {
+  createMemoryHistory,
+  createRootRoute as createRootRoute2,
+  createRouter,
+  Route,
+  RouterProvider,
+  createRoute as createRoute2,
+  RootRoute,
+  interpolatePath,
+  defaultStringifySearch
+} from "@tanstack/react-router";
+
+// src/routing/duplicateTree.tsx
+import React from "react";
+import { fn } from "storybook/test";
+import {
+  createRootRoute,
+  createRoute
+} from "@tanstack/react-router";
+function cloneChildRoute(oldRoute, parent, overrides, Story, storyRouteId) {
+  let { id: _id, getParentRoute: _g, ...childOpts } = oldRoute.options ?? {}, routeId = oldRoute.id ?? oldRoute.path, routeOverride = routeId ? overrides[routeId] : void 0, newRoute = createRoute({
+    ...childOpts,
+    loader: resolveOverride(routeOverride?.loader, childOpts.loader),
+    beforeLoad: resolveOverride(routeOverride?.beforeLoad, childOpts.beforeLoad),
+    ...routeOverride?.validateSearch ? { validateSearch: routeOverride.validateSearch } : {},
+    ...routeOverride?.loaderDeps ? { loaderDeps: routeOverride.loaderDeps } : {},
+    ...routeOverride?.context ? { context: routeOverride.context } : {},
+    ...Story && storyRouteId === routeId ? { component: () => React.createElement(Story, null) } : {},
+    getParentRoute: () => parent
+  });
+  newRoute.update({ id: oldRoute.id, path: oldRoute.path, fullPath: oldRoute.fullPath });
+  let children = oldRoute.children;
+  if (children?.length) {
+    let clonedChildren = children.map(
+      (child) => cloneChildRoute(child, newRoute, overrides, Story, storyRouteId)
+    );
+    newRoute._addFileChildren ? newRoute._addFileChildren(clonedChildren) : newRoute.addChildren(clonedChildren);
+  }
+  return newRoute;
+}
+function resolveOverride(override, original) {
+  if (override !== void 0)
+    return override;
+  if (original)
+    return fn(original).mockName(`route::${original.name || "anonymous"}`);
+}
+function duplicateTree({
+  routeTree,
+  overrides = {},
+  Story,
+  storyRouteId
+}) {
+  let rootOpts = routeTree.options ?? {}, rootOverride = overrides.__root__ ?? overrides[routeTree.id], newRoot = createRootRoute({
+    ...rootOpts,
+    loader: resolveOverride(rootOverride?.loader, rootOpts.loader),
+    beforeLoad: resolveOverride(rootOverride?.beforeLoad, rootOpts.beforeLoad),
+    ...rootOverride?.validateSearch ? { validateSearch: rootOverride.validateSearch } : {},
+    ...rootOverride?.loaderDeps ? { loaderDeps: rootOverride.loaderDeps } : {},
+    ...rootOverride?.context ? { context: rootOverride.context } : {}
+  }), children = routeTree.children;
+  if (children?.length) {
+    let clonedChildren = children.map(
+      (child) => cloneChildRoute(child, newRoot, overrides, Story, storyRouteId)
+    );
+    newRoot._addFileChildren ? newRoot._addFileChildren(clonedChildren) : newRoot.addChildren(clonedChildren);
+  }
+  return newRoot;
+}
+
+// src/routing/decorator.tsx
+function isRoute(value) {
+  return value instanceof Route || value instanceof RootRoute;
+}
+function getRouteFromContext(Story, context) {
+  let metaRoute = context.route, routerParameters = context.parameters.tanstack?.router ?? {}, routerParameterRoute = routerParameters.route, { route: _route, ...routeOverrides } = routerParameters ?? {};
+  if (routerParameters.routeTree)
+    return duplicateTree({
+      routeTree: routerParameters.routeTree,
+      overrides: routerParameters.routeOverrides,
+      Story,
+      storyRouteId: routerParameters.storyRouteId
+    });
+  let resolvedRoute = isRoute(routerParameterRoute) ? routerParameterRoute : isRoute(metaRoute) ? metaRoute : void 0;
+  if (resolvedRoute instanceof RootRoute) {
+    let clonedRoot = createRootRoute2({ ...resolvedRoute.options }), children = resolvedRoute.children;
+    if (children?.length) {
+      let clonedChildren = children.map((child2) => {
+        let { id: _id, getParentRoute: _g, ...childOpts } = child2.options ?? {};
+        return createRoute2({
+          ...childOpts,
+          ...routeOverrides,
+          component: () => React2.createElement(Story, null),
+          getParentRoute: () => clonedRoot
+        });
+      });
+      clonedRoot.addChildren(clonedChildren);
+    } else
+      clonedRoot.update({ component: () => React2.createElement(Story, null), ...routeOverrides });
+    return clonedRoot;
+  }
+  if (resolvedRoute instanceof Route) {
+    let root2 = createRootRoute2(), { id: _id, ...routeOpts } = resolvedRoute.options ?? {}, child2 = createRoute2({
+      ...routeOpts,
+      ...routeOverrides,
+      component: () => React2.createElement(Story, null),
+      getParentRoute: () => root2
+    });
+    return root2.addChildren([child2]), root2;
+  }
+  let root = createRootRoute2(), child = createRoute2({
+    component: () => React2.createElement(Story, null),
+    ...routerParameterRoute,
+    path: routerParameterRoute?.path ?? "/",
+    getParentRoute: () => root
+  });
+  return root.addChildren([child]), root;
+}
+function findRouteById(root, routeId) {
+  let stack = [root];
+  for (; stack.length; ) {
+    let route = stack.pop();
+    if (route.id === routeId)
+      return route;
+    let children = route.children;
+    children && stack.push(...children);
+  }
+}
+function createStoryRouter({ Story, context }) {
+  let routerParameters = context.parameters.tanstack?.router ?? {}, routeTree = getRouteFromContext(Story, context), inferredPath = routerParameters?.path || routeTree.children?.[0]?.fullPath || routeTree.children?.[0]?.path || routeTree.children?.[0]?.options?.path;
+  if (!inferredPath && routerParameters?.storyRouteId) {
+    let target = findRouteById(routeTree, routerParameters.storyRouteId);
+    inferredPath = target?.fullPath ?? target?.path ?? target?.options?.path;
+  }
+  let resolvedPath = interpolatePath({
+    path: inferredPath ?? "/",
+    params: routerParameters?.params ?? {}
+  }).interpolatedPath, search = routerParameters?.query ? defaultStringifySearch(routerParameters.query) : "";
+  search && (resolvedPath += search);
+  let history = createMemoryHistory({
+    initialEntries: [resolvedPath]
+  });
+  return history.replace(resolvedPath), createRouter({
+    routeTree,
+    history,
+    defaultNotFoundComponent(props) {
+      return React2.createElement("div", null, "Route not found: ", props.routeId);
+    }
+  });
+}
+var tanstackRouteDecorator = (Story, context) => {
+  let router = createStoryRouter({
+    Story,
+    context
+  });
+  return React2.createElement(RouterProvider, { router });
+};
+
+// src/routing/loader.ts
+import { Route as Route2, RootRoute as RootRoute2 } from "@tanstack/react-router";
+function isRoute2(value) {
+  return value instanceof Route2 || value instanceof RootRoute2;
+}
+function getComponentFromRoute(route) {
+  if (route.options?.component)
+    return route.options.component;
+  if (route instanceof RootRoute2)
+    return route.children?.[0]?.options?.component;
+}
+var routeComponentLoader = (context) => {
+  let componentRoute = isRoute2(context.component) ? context.component : void 0, routerParameters = context.parameters.tanstack?.router ?? {}, resolvedRoute = (isRoute2(routerParameters.route) ? routerParameters.route : void 0) ?? componentRoute;
+  if (resolvedRoute) {
+    if (!context.component) {
+      let component = getComponentFromRoute(resolvedRoute);
+      component && (componentRoute || !context.component) && (context.component = component);
+    }
+    context.route || (context.route = resolvedRoute);
+  }
+};
+
+// src/preview.tsx
+var loaders = [routeComponentLoader], decorators = [tanstackRouteDecorator], parameters = {
+  tanstack: {}
+};
+
+export {
+  loaders,
+  decorators,
+  parameters,
+  preview_exports
+};
diff --git a/dist/export-mocks/react-router.d.ts b/dist/export-mocks/react-router.d.ts
deleted file mode 100644
index 50a7de0afe6922db475314bbb9db6e989ede816e..0000000000000000000000000000000000000000
diff --git a/dist/export-mocks/start-server.d.ts b/dist/export-mocks/start-server.d.ts
deleted file mode 100644
index 09153c1bb210db36368c0b6d54664661eb7343df..0000000000000000000000000000000000000000
diff --git a/dist/export-mocks/start-storage-context.d.ts b/dist/export-mocks/start-storage-context.d.ts
deleted file mode 100644
index 91ee396b96906d893cc4e831c290403f39454b50..0000000000000000000000000000000000000000
diff --git a/dist/export-mocks/start.d.ts b/dist/export-mocks/start.d.ts
deleted file mode 100644
index 36cf3ebb5a1c9d975b7f4e9c95c38893eee4aa07..0000000000000000000000000000000000000000
diff --git a/dist/index.d.ts b/dist/index.d.ts
deleted file mode 100644
index bbbdd16259d2ff8d795b9409fc9f86fe4b1fed15..0000000000000000000000000000000000000000
diff --git a/dist/index.js b/dist/index.js
index 0ac08184c47c59157109099bc7b9856153f6b66b..fbd1121852bc03e7cb0fd4e6702efcedc928a8c3 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1,6 +1,6 @@
 import {
   preview_exports
-} from "./_browser-chunks/chunk-DWOHA7QX.js";
+} from "./_browser-chunks/chunk-RT3VPW6U.js";
 import "./_browser-chunks/chunk-4BE7D4DS.js";
 
 // src/index.ts
diff --git a/dist/node/index.d.ts b/dist/node/index.d.ts
deleted file mode 100644
index bce45f981fbc9c24328e7dabbfae75f95c71604b..0000000000000000000000000000000000000000
diff --git a/dist/node/index.js b/dist/node/index.js
index 3c8b40c93329a1b894ac2268fd01fd0a3734dba4..5f02dfc321829b7e06dbf1fc681f112fec24d37a 100644
--- a/dist/node/index.js
+++ b/dist/node/index.js
@@ -1,10 +1,10 @@
-import CJS_COMPAT_NODE_URL_hvuj3m650wv from 'node:url';
-import CJS_COMPAT_NODE_PATH_hvuj3m650wv from 'node:path';
-import CJS_COMPAT_NODE_MODULE_hvuj3m650wv from "node:module";
+import CJS_COMPAT_NODE_URL_3rrkrsepp9x from 'node:url';
+import CJS_COMPAT_NODE_PATH_3rrkrsepp9x from 'node:path';
+import CJS_COMPAT_NODE_MODULE_3rrkrsepp9x from "node:module";
 
-var __filename = CJS_COMPAT_NODE_URL_hvuj3m650wv.fileURLToPath(import.meta.url);
-var __dirname = CJS_COMPAT_NODE_PATH_hvuj3m650wv.dirname(__filename);
-var require = CJS_COMPAT_NODE_MODULE_hvuj3m650wv.createRequire(import.meta.url);
+var __filename = CJS_COMPAT_NODE_URL_3rrkrsepp9x.fileURLToPath(import.meta.url);
+var __dirname = CJS_COMPAT_NODE_PATH_3rrkrsepp9x.dirname(__filename);
+var require = CJS_COMPAT_NODE_MODULE_3rrkrsepp9x.createRequire(import.meta.url);
 
 // ------------------------------------------------------------
 // end of CJS compatibility banner, injected by Storybook's esbuild configuration
diff --git a/dist/preset.d.ts b/dist/preset.d.ts
deleted file mode 100644
index 85058bd8ba11c51578e4a2853a0184e1337cf18f..0000000000000000000000000000000000000000
diff --git a/dist/preset.js b/dist/preset.js
index 2bdd2f5cc023c13c0bf4ca5257df233df12932d7..d25ea23aacc6b01ba19a00720b7199bfb074a06f 100644
--- a/dist/preset.js
+++ b/dist/preset.js
@@ -1,10 +1,10 @@
-import CJS_COMPAT_NODE_URL_hvuj3m650wv from 'node:url';
-import CJS_COMPAT_NODE_PATH_hvuj3m650wv from 'node:path';
-import CJS_COMPAT_NODE_MODULE_hvuj3m650wv from "node:module";
+import CJS_COMPAT_NODE_URL_3rrkrsepp9x from 'node:url';
+import CJS_COMPAT_NODE_PATH_3rrkrsepp9x from 'node:path';
+import CJS_COMPAT_NODE_MODULE_3rrkrsepp9x from "node:module";
 
-var __filename = CJS_COMPAT_NODE_URL_hvuj3m650wv.fileURLToPath(import.meta.url);
-var __dirname = CJS_COMPAT_NODE_PATH_hvuj3m650wv.dirname(__filename);
-var require = CJS_COMPAT_NODE_MODULE_hvuj3m650wv.createRequire(import.meta.url);
+var __filename = CJS_COMPAT_NODE_URL_3rrkrsepp9x.fileURLToPath(import.meta.url);
+var __dirname = CJS_COMPAT_NODE_PATH_3rrkrsepp9x.dirname(__filename);
+var require = CJS_COMPAT_NODE_MODULE_3rrkrsepp9x.createRequire(import.meta.url);
 
 // ------------------------------------------------------------
 // end of CJS compatibility banner, injected by Storybook's esbuild configuration
@@ -13,7 +13,188 @@ var require = CJS_COMPAT_NODE_MODULE_hvuj3m650wv.createRequire(import.meta.url);
 // src/preset.ts
 import { fileURLToPath } from "node:url";
 import { viteFinal as reactViteFinal } from "@storybook/react-vite/preset";
-import { defineEnv } from "unenv";
+
+// src/plugins/server-code-elimination.ts
+import { transformSync } from "@babel/core";
+import {
+  isImportDeclaration,
+  isIdentifier,
+  isImportSpecifier,
+  isMemberExpression,
+  isCallExpression,
+  isExpression,
+  identifier,
+  stringLiteral,
+  callExpression,
+  importDeclaration,
+  importSpecifier
+} from "@babel/types";
+var SERVER_FN_RE = /\bcreateServerFn\b/, MIDDLEWARE_RE = /\bcreateMiddleware\b/, ISOMORPHIC_FN_RE = /\bcreateIsomorphicFn\b/, SERVER_ONLY_FN_RE = /\bcreateServerOnlyFn\b/, CLIENT_ONLY_FN_RE = /\bcreateClientOnlyFn\b/, ANY_PATTERN_RE = /\b(createServerFn|createMiddleware|createIsomorphicFn|createServerOnlyFn|createClientOnlyFn)\b/;
+function serverCodeEliminationPlugin() {
+  return {
+    name: "storybook:tanstack-react:server-code-elimination",
+    enforce: "pre",
+    transform: {
+      // we can fully rely on transform.filter
+      // and not worry about the handler since tanstack start users are Vite > 8 only
+      filter: {
+        id: {
+          include: [/\.tsx?$/],
+          // todo make export-mocks more robust (currently we just exclude all files in that folder, but ideally we should only exclude the relevant ones)
+          exclude: [/node_modules/, /export-mocks/]
+        },
+        code: ANY_PATTERN_RE
+      },
+      async handler(code, id) {
+        if (!/\.[mc]?[jt]sx?$/.test(id) || !ANY_PATTERN_RE.test(code))
+          return null;
+        let state = { code, modified: !1, needsFnImport: !1 }, result = transformSync(code, {
+          filename: id,
+          sourceType: "module",
+          parserOpts: {
+            plugins: ["typescript", "jsx"]
+          },
+          plugins: [() => serverCodeElimination(state)],
+          sourceMaps: !0,
+          configFile: !1,
+          babelrc: !1
+        });
+        return !state.modified || !result?.code ? null : {
+          code: result.code,
+          map: result.map
+        };
+      }
+    }
+  };
+}
+function serverCodeElimination(state) {
+  function sbFnCall() {
+    return state.needsFnImport = !0, buildFnCall();
+  }
+  function sbFnCallWithImpl(impl) {
+    return state.needsFnImport = !0, buildFnCallWithImpl(impl);
+  }
+  return {
+    visitor: {
+      Program(programPath) {
+        let tanstackImports = collectTanstackImports(programPath.node.body), resolves = (name, factory) => resolvesToFactory(tanstackImports, name, factory);
+        programPath.traverse({
+          CallExpression(path) {
+            let node = path.node;
+            if (isIdentifier(node.callee) && resolves(node.callee.name, "createServerOnlyFn") && SERVER_ONLY_FN_RE.test(state.code)) {
+              path.replaceWith(sbFnCall()), state.modified = !0;
+              return;
+            }
+            if (isIdentifier(node.callee) && resolves(node.callee.name, "createClientOnlyFn") && CLIENT_ONLY_FN_RE.test(state.code)) {
+              let innerFn = node.arguments[0];
+              innerFn && isExpression(innerFn) && (path.replaceWith(sbFnCallWithImpl(innerFn)), state.modified = !0);
+              return;
+            }
+            let methodName = getMethodName(node);
+            if (!methodName)
+              return;
+            let root = findChainRoot(node);
+            if (root) {
+              if (methodName === "handler" && resolves(root.rootName, "createServerFn") && SERVER_FN_RE.test(state.code)) {
+                let handlerArg = node.arguments[0];
+                if (handlerArg) {
+                  if (isIdentifier(handlerArg)) {
+                    let binding = path.scope.getBinding(handlerArg.name);
+                    binding && binding.path.remove();
+                  }
+                  node.arguments[0] = sbFnCall();
+                }
+                state.modified = !0;
+                return;
+              }
+              if (resolves(root.rootName, "createMiddleware") && MIDDLEWARE_RE.test(state.code)) {
+                (methodName === "server" || methodName === "inputValidator") && isMemberExpression(path.node.callee) && (path.replaceWith(path.node.callee.object), state.modified = !0);
+                return;
+              }
+              if (resolves(root.rootName, "createIsomorphicFn") && ISOMORPHIC_FN_RE.test(state.code)) {
+                if (methodName === "client") {
+                  let innerFn = node.arguments[0];
+                  innerFn && isExpression(innerFn) && (path.replaceWith(sbFnCallWithImpl(innerFn)), state.modified = !0);
+                  return;
+                }
+                if (methodName === "server") {
+                  let parent = path.parent;
+                  (!isMemberExpression(parent) || !isCallExpression(path.parentPath?.parent)) && (path.replaceWith(sbFnCall()), state.modified = !0);
+                }
+                return;
+              }
+            }
+          }
+        }), state.modified && (state.needsFnImport && programPath.node.body.unshift(buildFnImport()), eliminateDeadImports(programPath));
+      }
+    }
+  };
+}
+function buildFnImport() {
+  return importDeclaration(
+    [importSpecifier(identifier("__sb_fn"), identifier("fn"))],
+    stringLiteral("storybook/test")
+  );
+}
+function buildFnCall() {
+  return callExpression(identifier("__sb_fn"), []);
+}
+function buildFnCallWithImpl(impl) {
+  return callExpression(identifier("__sb_fn"), [impl]);
+}
+function collectTanstackImports(body) {
+  let imports = /* @__PURE__ */ new Map();
+  for (let node of body) {
+    if (!isImportDeclaration(node))
+      continue;
+    let src = node.source.value;
+    if (!(!src.includes("@tanstack/") && !src.includes("export-mocks") && !src.includes("@storybook/tanstack-react"))) {
+      for (let spec of node.specifiers)
+        if (isImportSpecifier(spec)) {
+          let importedName = isIdentifier(spec.imported) ? spec.imported.name : spec.imported.value;
+          imports.set(spec.local.name, importedName);
+        }
+    }
+  }
+  return imports;
+}
+function resolvesToFactory(imports, name, factoryName) {
+  return name === factoryName || imports.get(name) === factoryName;
+}
+function findChainRoot(node) {
+  let current = node;
+  for (; ; ) {
+    let callee = current.callee;
+    if (isIdentifier(callee))
+      return { rootCall: current, rootName: callee.name };
+    if (isMemberExpression(callee) && isCallExpression(callee.object)) {
+      current = callee.object;
+      continue;
+    }
+    return null;
+  }
+}
+function getMethodName(node) {
+  return isMemberExpression(node.callee) && isIdentifier(node.callee.property) ? node.callee.property.name : null;
+}
+function eliminateDeadImports(programPath) {
+  let referencedIdentifiers = /* @__PURE__ */ new Set();
+  programPath.traverse({
+    enter(path) {
+      let { node } = path;
+      isIdentifier(node) && !path.isBindingIdentifier() && referencedIdentifiers.add(node.name);
+    }
+  }), programPath.traverse({
+    ImportDeclaration(path) {
+      let specifiers = path.node.specifiers.filter(
+        (spec) => referencedIdentifiers.has(spec.local.name)
+      );
+      specifiers.length === 0 ? path.remove() : specifiers.length !== path.node.specifiers.length && (path.node.specifiers = specifiers);
+    }
+  });
+}
+
+// src/preset.ts
 var INTERCEPTED_PATTERNS = ["virtual:cloudflare", "server-entry", "worker-entry"], INTERCEPTED_MODULES = ["@tanstack/react-start"], START_SERVER_MODULES = [
   "@tanstack/react-start/server",
   "@tanstack/react-start-server",
@@ -32,13 +213,7 @@ var INTERCEPTED_PATTERNS = ["virtual:cloudflare", "server-entry", "worker-entry"
   ...entry,
   fileURLToPath(import.meta.resolve("@storybook/tanstack-react/preview"))
 ], viteFinal = async (config, options) => {
-  let { env } = defineEnv({
-    nodeCompat: !0,
-    npmShims: !0,
-    resolve: !0,
-    overrides: {},
-    presets: []
-  }), { alias, inject, external, polyfill } = env, reactConfig = await reactViteFinal(config, options), isTanStackStartPlugin = (p) => {
+  let reactConfig = await reactViteFinal(config, options), isTanStackStartPlugin = (p) => {
     if (Array.isArray(p))
       return p.some(isTanStackStartPlugin);
     let pluginRecord = p;
@@ -49,6 +224,7 @@ var INTERCEPTED_PATTERNS = ["virtual:cloudflare", "server-entry", "worker-entry"
     import.meta.resolve("@storybook/tanstack-react/react-router")
   ), plugins = [
     ...(reactConfig.plugins ?? []).filter((p) => !isTanStackStartPlugin(p)),
+    serverCodeEliminationPlugin(),
     {
       name: "tanstack-start-plugin-remover",
       enforce: "pre",
@@ -79,11 +255,7 @@ var INTERCEPTED_PATTERNS = ["virtual:cloudflare", "server-entry", "worker-entry"
               "@tanstack/react-start-server",
               "@tanstack/start-server-core"
             ]
-          },
-          resolve: {
-            alias
-          },
-          external
+          }
         };
       }
     }
diff --git a/dist/preview.d.ts b/dist/preview.d.ts
deleted file mode 100644
index 7de76c9a125d6bc78e9387e51d70f329797d32f8..0000000000000000000000000000000000000000
diff --git a/dist/preview.js b/dist/preview.js
index bff387e58238cb8885366831ed55ca5c54c73dfb..21c3e200198bcd61604426453321ae58b9f5b2ef 100644
--- a/dist/preview.js
+++ b/dist/preview.js
@@ -2,7 +2,7 @@ import {
   decorators,
   loaders,
   parameters
-} from "./_browser-chunks/chunk-DWOHA7QX.js";
+} from "./_browser-chunks/chunk-RT3VPW6U.js";
 import "./_browser-chunks/chunk-4BE7D4DS.js";
 export {
   decorators,
